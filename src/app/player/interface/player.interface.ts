export interface playerObj {
  player: playerData
}

export interface playerData {
  nombre: string,
  edad: number,
  numero: number,
  equipo: string,
  uid: string
}

export interface playerTable {
  nombre: string,
  edad: number,
  numero: number,
}
